 <h2>Peticion:  <%= @form.id %> </h2>
  <div> <%= link_to '<button class="btn btn-danger" > Eliminar</button>'.html_safe, form_path(@form),
                                                                                  method: :delete,
                                                                                  data: { confirm: 'Desea continuar?' } %>
                                                                                  </div>

<br>
<br>            
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Descripcion</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @form.descripcion %></td>
    </tbody>
  </table>